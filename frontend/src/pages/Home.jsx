export default function Home() {
  return (
    <section aria-labelledby="home-title">
      <h1 id="home-title">Welcome to SmartChair</h1>
      <p>
        This is the starting point for the frontend. Pages, components, and
        integration points live under <code>src/</code>.
      </p>
      <section aria-labelledby="quick-start">
        <h2 id="quick-start">Quick Start</h2>
        <ol>
          <li>Run `npm install` in the `frontend` folder.</li>
          <li>Start dev server: `npm run dev`.</li>
        </ol>
      </section>
    </section>
  )
}
